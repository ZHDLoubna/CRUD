{{ include ('Pages/inc/header.html.twig') }}

<div class="container">
    <a href="{{ absolute_url(asset('')) }}app_dev.php/post " class="btn btn-success btn-sm">Back</a>
    <h3>edit Post</h3>
    {{form_start(form) }}
    {{form_widget(form) }}
    {{form_end(form) }}
</div>
